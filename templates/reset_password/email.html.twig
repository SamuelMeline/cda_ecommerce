<h1>Salut !</h1>

<p>Pour rénitialiser votre mot de passe cliquez sur ce lien :</p>

{# Redirection vers http://127.0.0.1:8000/ #}

<a href="http://127.0.0.1:8000{{ path('app_reset_password', {token: resetToken.token}) }}">{{ path('app_reset_password', {token: resetToken.token}) }}</a>


<p>This link will expire in {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.</p>

<p>Cheers!</p>
