{% extends 'base.html.twig' %}

{% block title %}New Categories{% endblock %}

{% block body %}
<div class="container">
    <h1>Create new Categories</h1>

    {{ include('categories/_form.html.twig') }}

    <a class="btn btn-secondary" href="{{ path('app_categories_index') }}">Retour a la liste</a>
</div>
{% endblock %}